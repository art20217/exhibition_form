# 更新紀錄

會談紀錄表的版本沿革。最新版在最上。

各版的「為什麼」——踩到什麼、怎麼發現的——多半也寫在對應的 commit 訊息裡；
這份文件記錄結果與對既有資料的影響。

**發布新版時**：`index.html` 的版本字串與 `sw.js` 的 `VERSION` 必須一致，
否則新的 HTML 會沿用舊的快取名稱，所有平板都會停在舊版且無從察覺
（`tests/e2e-pwa.js` 有一條斷言擋這件事）。

---

## v3.9.0 更新重點

- **編輯紀錄改為摘要列＋選項彈窗：** 編輯一筆紀錄時，客戶資料／客戶需求／公司背景三組欄位會串成同一頁——以預設欄位計算是 21 個欄位、**約 130 個選項按鈕**，改一個電話號碼得捲過所有機器品牌與行業別。現在**選項類欄位只顯示已選中的值**，右側一個「編輯」鈕開啟彈窗修改該欄位的選項。
  - **只折疊選項類欄位**（多選、單選、下拉、國籍選單）。文字、Email、電話、日期、備註、星等維持行內直接編輯——它們沒有選項可隱藏，改個錯字不該多點兩下。
  - 未填的欄位顯示灰字「未填」而不是消失，**快速填單留下的空白公司背景就是靠這裡補齊**。
  - 彈窗的「取消」會**還原整個欄位群組的快照**，不是只還原單一鍵——「其他」的文字存在 `<id>__otherText` 與 `<id>__otherText__<分類>` 這些側鍵，只還原一個鍵會留下殘值。
  - **客戶填單的三步驟流程完全不受影響**，選項照常全部攤開。測試有專門一條守住這件事：把填單頁也折疊起來會是退步，不是功能。

## v3.8.0 更新重點

- **App 更名為「會談紀錄表」**（`<title>`、PWA manifest、README）。主畫面圖示下的短名維持 `Exhibitions`。
- **新增「客戶資料紀錄」瀏覽頁：** 從活動內頁進入、需輸入 PIN。這是一個**獨立畫面，不是後台資料紀錄的捷徑**——接待人員可以搜尋（姓名／公司／接待人員）與編輯，但**沒有刪除、沒有匯出、沒有清除全部**。編輯完回到瀏覽頁而非後台，所以沒人會落在破壞性操作旁邊一步之遙。
  > 它與後台共用同一組 PIN，因此上述限制是**流程上的區隔而非權限上的**：拿到 PIN 的人一樣能自己進「表單管理」刪除或匯出。要真正分權需要第二組 PIN。
- **新增「快速填單」入口：** 只填客戶資料與客戶需求，**跳過公司背景**，步驟顯示 1 / 2、2 / 2。紀錄照常寫入，`companyFields` 留空，日後可從瀏覽頁或後台補齊。原本的兩個入口不變，仍是三步驟。
- **訪客國籍改為可搜尋的國家選單：** 60 個貿易往來國家，中英雙語，輸入即時過濾（打「德」或「Germ」都找得到德國）。**清單外的國名照樣可以直接輸入並存檔**，所以既有紀錄裡的自由文字值（如 `TW`）不會失效。儲存的是英文國名，與其他選項欄位一致。
- **齒輪按鈕加上文字標籤：** 活動列表為「活動管理」、活動內頁為「表單管理」。窄螢幕下只留圖示，避免擠壓活動名稱。
- **已結束的活動只在「活動管理」中顯示：** 未解鎖時整個「已結束」區塊不存在，日常使用不會被舊活動擋路。
- **預設填單人員改為實際團隊 8 人：** Charlene 蘇秋菊、Will 黃柏儒、Steve 陳誌翔、Nadia 鄭淑卿、Eric 顏耀中、Alen 黃世仰、Wing 張詩穎、Rick 張瑞育。由 `migratedTo38` 旗標一次性覆蓋所有既有活動（同 v3.6 的作法，後台自訂過的名單也會被覆蓋）。
- **活動編輯的日期欄位改為一行一格：** 並排時日期輸入框的原生選擇器有最小寬度，50% 的欄位容納不下，平板上會撐出 modal。

### 開發者注意：dc-runtime 沒有 pointer 事件

模板中寫 `onpointerdown` **不會生效**。`EVENT_MAP` 沒有這個項目，fallback 產生的是 `onPointerdown`（大小寫錯誤），React 直接忽略——處理器不會執行，也不會報錯。國籍選單的選項一度就是這樣靜靜地失效。可用的事件請參考 `EVENT_MAP`。

## v3.7.0 更新重點 — PWA 離線化（Phase 1 前半段）

- **可安裝為主畫面 App（standalone）：** 加入 `manifest.webmanifest` 與 icon（含 Android 圓形遮罩用的 maskable 版本），圖示沿用活動首頁頁首的日曆符號。iOS 的狀態列刻意維持不透明（`default`）——`black-translucent` 會讓白色狀態列文字疊在 body 的安全區留白上，而那塊是淺色頁面背景，等於看不見。
- **Service Worker 離線快取：** precache 就是整個站（`index.html` + manifest + icons），因為其他依賴全都內嵌在單一 HTML 裡。首次連網開啟後，完全離線也能正常啟動。
- **更新採橫幅、絕不自動重整：** 新版本安裝完成後**停在 `waiting` 不接管**（沒有 `skipWaiting()`）。橫幅只存在於活動首頁的區塊裡，**進入表單後它在 DOM 中根本不存在**——不是靠條件隱藏，是結構上不可能出現。客戶填到一半的畫面永遠不會在腳下被重整。
- **`?sw=off` 緊急停用（`?sw=on` 復原）：** 面對展場中拿不到手、卡在壞快取的平板，這是唯一的救援途徑。停用狀態**持久化**於 `localStorage`——第一版做成一次性的，結果解除註冊後 reload 又立刻註冊回去，若壞的正是 SW 本身等於毫無用處。停用期間活動首頁會標示，避免平板悄悄停在只能連線的狀態。**不會動到 IndexedDB**，紀錄完好。
- **只接管自己的入口路徑：** SW 不會把 scope 底下所有 navigation 都回答成 app 外殼——這個 app 只有一個入口、沒有前端路由，其他路徑照常走網路。

> **Background Sync 不在此版**。Phase 1 原訂的「偵測到網路後自動同步至後端」有兩重阻塞：後端屬於 Phase 2 尚未存在；而且 **Safari／iOS 從未實作 Background Sync API**，到 iOS 26.5 仍然沒有。展場裝置是 iPad，這條路在該平台不通，日後需改為前景同步（載入時、`visibilitychange` 回前景時、或 `online` 事件）。

### 測試基礎建設

- 16 支端到端測試與 CI **納入版控**（先前只存在於開發用的暫存目錄，曾整批遺失）。詳見下方「測試」一節。
- 新增 `e2e-pwa.js`（26 條斷言）：離線可用、更新橫幅不自動重整且不出現在表單頁、`?sw=off/on` 往返、紀錄不受影響、`index.html` 與 `sw.js` 的版本字串必須一致（`sw.js` 沒有 build step，這是唯一擋得住忘記 bump 快取版本的機制）。

## v3.6.1 更新重點

- **修正手機無法輸入後台 PIN 碼：** 點輸入框後鍵盤會跳出來，但框內完全沒有反應、盲打也進不去。原因是 v3.5 為了擋掉密碼管理圖示而加的一條規則：
  ```css
  #pin-input::-webkit-textfield-decoration-container { display: none !important; }
  ```
  `-webkit-textfield-decoration-container` **不是圖示**，而是 WebKit 用來包住輸入框 **inner editor**（真正可輸入的區域）的容器，隱藏它等於把可輸入區一起拿掉——欄位仍能取得焦點（所以鍵盤會跳出來），但沒有東西可顯示、也沒有東西可打字。已移除該行；針對兩個 autofill 按鈕的規則保留，那些是真正的裝飾按鈕。
- **為什麼桌機沒事、測試也沒抓到：** 桌機 Chrome 的密碼欄位沒有裝飾按鈕，而且 **Blink 根本沒有實作這個 pseudo-element**，規則等於空轉；iOS Safari 則會替密碼欄位加上 strong-password／autofill 按鈕，容器因此存在、欄位就壞掉。加上所有測試都用 Playwright 的 `fill()`——它直接指派 `.value` 再派發一次 `input` 事件，**完全不經過 inner editor**，所以就算在會壞的瀏覽器上也照樣通過。測試已全面改為逐鍵輸入（`pressSequentially`），並新增 `e2e-pin-mobile.js`（含一條「不得再出現這個選擇器」的靜態防護，因為 headless Chromium 重現不了 iOS 的實際行為）。

## v3.6 更新重點

- **預設接待人員換成 Esme（陳佩昀）、Crystal（宋佳蓉）：** 舊的三個姓名全部移除。
- **既有活動的名單由 `migratedTo36` 旗標一次性無條件覆蓋：** 名單是存在每個活動自己的 `staff` 裡的資料，改預設值不會動到已建立的活動，所以加一次性遷移把所有活動一律換成新名單。與 v3.5 的選項補寫不同，這裡是**整份取代而非合併**——舊的三個名字是要移除、不是被補充，合併只會把它們留著。**後台自訂過的名單也一併覆蓋**（這是刻意選的行為）。遷移**之後**才改的名單則受旗標保護，重新載入不會被打回預設。
- **既有紀錄不受影響：** 紀錄存的是接待人員的英文字串本身，不是指向活動名單的參照，所以在舊名單下填的紀錄，資料紀錄與匯出都照常顯示原本的名字。

> 注意：pre-v3.4 的資料庫升上來時，v3.4 原本會把舊 `greeter` 選項（含後台自訂的姓名）帶進第一個活動的名單，但 v3.6 緊接著在同一次載入中覆蓋掉——**這條路徑上的自訂姓名不會保留**。

## v3.5 更新重點

- **後台版面統一：** 欄位頁籤（800px）、資料紀錄（滿版）、系統設定（640px）三種不同寬度收斂為單一的 900px（`--admin-w`），頁籤列也跟著置中對齊，**第一個頁籤的文字與內容左緣落在同一條線上**。資料紀錄不再拉滿整個視窗，三個欄位欄各約 180px。
- **頁籤列改為捲動區內的 sticky 元素：** 傳統（非疊加式）捲軸會佔掉捲動容器的內容寬度，因此**內容夠長而出現捲軸的頁籤**（公司背景欄位、系統設定）會比其他頁籤往左偏約半個捲軸寬，看起來像寬度不一致。改為頁籤列與內容共用同一個捲動容器、以 `position: sticky` 固定在頂端，任何捲軸都會同時影響兩者；再加上 `scrollbar-gutter: stable` 永遠保留捲軸空間，切換頁籤時版面完全不跳動。
- **移除「業務備註欄位」頁籤與業務備註頁：** 接待人員與訪談日期已改由活動設定，這個頁籤沒有可管理的內容了。連帶移除交接後的業務備註畫面——交接畫面固定顯示「完成，返回」直接結束。既有紀錄中的舊值仍照常匯出。
- **活動首頁的版本號常駐底部：** 先前它跟在清單末端，活動只有一兩個時會浮到畫面上方；改為以 `margin-top: auto` 推到底，不論活動多寡都貼在最下方。
- **PIN 輸入框比例修正：** 原本固定 160px 配 2rem 字級與 12px 字距，四位數字本來就塞得很緊；部分裝置還會在框內加上顯示／密碼管理圖示而被撐爆。改為彈性寬度（上限 220px）、較小字級，並以 `text-indent` 補正字距造成的置中偏移，同時隱藏各瀏覽器自帶的密碼圖示。
- **洽談事由新增選項：** 機器瞭解、分享市場或同業訊息、打招呼聊天，並開啟「其他，請填寫」。
- **公司型態新增選項：** 代工廠（OEM Factory）。
- **修正 pre-v3.1 資料庫無法開啟：** 成交機率與感興趣產品搬到客戶需求群組後，v3.1 遷移仍到舊的 Staff 預設值裡找它們的選項，`find()` 回傳 `undefined`、讀 `.options` 直接拋錯，整個資料庫因此打不開。改為向 `getDefaultNeedsFields()` 查詢。只影響「成交機率仍是星等評分」或「感興趣產品仍是空白選項」的舊資料庫。

## v3.4 更新重點

**單一展覽的表單，變成可以重複用在每一場展覽的工具。**

- **活動首頁成為根畫面：** 開啟後先看到活動列表，分「使用中」與「已結束」兩區，每張卡片顯示活動名稱、日期區間、接待人員數與紀錄筆數。點進去才是該活動的表單。
- **新增／編輯／結束／刪除活動需要 PIN：** 展場平板放在客戶面前，管理按鈕預設隱藏，點右上角齒輪輸入 PIN 後才會出現。新增活動可設定活動名稱、活動日期區間、接待人員名單（英文＋中文兩欄），並選擇表單欄位來源——**內建預設**或**複製自某個既有活動**（複製的是獨立副本，日後修改互不影響）。
- **每場活動的欄位與資料完全獨立：** 欄位定義以 `<活動 id>::<欄位群組>` 為鍵分別儲存，每筆紀錄帶 `eventId`。後台的欄位設定、資料紀錄、匯出、「清除全部紀錄」一律只作用於目前開啟的活動。
- **填單人員與填單日期改在活動頁選擇：** 進入活動後，業務先選自己的名字（**可複選兩人以上**）與填單日期，才能點開兩個表單入口；**沒選就進不去**。填完自動帶入該筆紀錄，並在頁面上方持續顯示「填單人員：… ｜ 填單日期：…」與「重新選擇」，同一位業務連續接待時不必重選，換人時一眼看得出要改。
- **接待人員改為複選、由活動供應：** `greeter` 由單選改為多選，選項不再於後台編輯，改由活動的接待人員名單提供；`visit_date` 同樣改由活動頁帶入。兩個欄位定義仍保留（所以資料紀錄的「接待人員」欄與匯出欄位都還在），在後台標示為**由活動設定**、不可編輯或刪除。
- **業務備註頁只在還有業務專屬欄位時出現：** 上述兩欄搬走後，預設情況下業務備註頁已無內容，交接畫面直接顯示「完成，返回」。若在後台為該群組新增自訂欄位，業務入口會自動回來。
- **已結束的活動：** 兩個表單入口停用，但仍可進入後台檢視與匯出資料。結束／重新啟用是可復原的動作，只需一層確認；刪除活動仍是兩層＋輸入 `DELETE`。
- **匯出加上 Event 欄、檔名帶活動名稱：** `export_<活動名稱>_YYYYMMDD_HHMMSS.zip`。
- **系統設定維持全域：** 裝置名稱、管理者 PIN、GDPR 條文適用於所有活動。

## v3.3 更新重點

- **後台全面中文化：** 後台管理的所有介面文字（頁籤、欄位清單、欄位編輯器、資料紀錄、系統設定、提示訊息、表格標題、刪除確認）改為純中文。欄位類型以中文顯示（單行文字／單選按鈕組／多選核取組／日期…），紀錄表格改用欄位中文名稱。欄位的英文名稱仍保留並顯示為次要資訊——匯出欄名與選項比對仍依賴它。
- **所有刪除作業改為兩層確認：** 單筆欄位刪除、單筆紀錄刪除、清除全部紀錄、移除名片照片，四處全部改為「第 1 層說明要刪什麼 → 第 2 層再次確認無法復原」才會執行。先前單筆欄位與單筆紀錄是按下即刪、完全沒有確認。清除全部仍需先輸入 `DELETE` 才能進入第 2 層。
- **訪客國籍、語言移到客戶資料頁：** 這兩個欄位從業務備註頁移至客戶資料填寫頁。
- **感興趣產品、成交機率、備註移到客戶需求頁：** 客戶需求頁欄位順序為 洽談事由 → 感興趣產品 → 成交機率 → 備註。原「潛力評級」更名為**成交機率**（欄位 id 不變，舊紀錄的值照常顯示），七級 A～G 選項不變。
- **名片拍攝移到客戶資料頁尾：** 名片拍照從業務備註頁移至客戶資料填寫頁的頁尾（GDPR 同意聲明上方），與該頁配色一致。業務備註頁在搬移後只保留接待人員與訪談日期兩個欄位，交接畫面與整體流程維持不變。

> 註：成交機率現在位於客戶端表單上。此版本假設平板由業務人員操作或陪同填寫；若改為讓客戶單獨自行填表，客戶會看到這個內部評估欄位。

## v3.2 更新重點

- **三步驟流程順序調整：** Company Profile 公司背景資料改為**固定在第 2 步**（不論先選「客戶資料」或「客戶需求」），先選的維持第 1 步、後選的變成第 3 步。
- **checkbox-group 新增「分組獨立其他」機制（`perGroupOther`）：** 「使用機器」欄位的歐洲／台灣／大陸／韓國／新增「本土」五個分類子區塊，現在**各自擁有獨立的「其他，請填寫」**（先前僅整個欄位共用一個附加在最後）。此機制為通用能力，後台任何 checkbox-group 欄位皆可透過「每個分組獨立其他」勾選框與「額外空白分類」設定啟用（供未來其他分組欄位使用）。
- **接待人員新增「其他」：** 業務備註「接待人員」欄位保留既有三個姓名選項（後台仍可編輯），新增「其他，請填寫」因應臨時支援的人員。
- **新增通用 `date` 欄位類型：** 「訪談日期」由原本硬編三個日期的單選按鈕，改為原生日期選擇器；空白時預設當天日期，可自由調整。後台欄位編輯器 Type 下拉新增「Date」選項，供任何表單新增日期欄位使用。
- **選項按鈕固定寬度：** 短標籤選項（雙語合計 ≤40 字）改為固定寬度（200px）並排列成整齊網格；長文字說明選項（如規模型態）仍維持全寬整行顯示，不受影響。

## v3.1 更新重點

- **第三個客戶端表單「Company Profile 公司背景資料」：** 依 TIENKANG 展覽會談記錄表加入行業別、客戶類型、公司型態、公司產品、使用機器、股份型態、營業額、員工人數、產能、規模型態等公司背景欄位。非主畫面可選項，固定插入三步驟流程中（v3.2 起固定為第 2 步，見上）。
- **新增「單選按鈕組」(radio-group) 欄位類型：** 外觀與 checkbox-group 相同的大按鈕，但點選會自動取消其他選項，並可顯示長段說明文字（如規模型態）。
- **「其他，請填寫」：** checkbox-group 與 radio-group 皆可在後台開啟，選取「其他」後即時出現文字輸入框，資料與匯出皆以 `Other: <內容>` 呈現。
- **選項分類子區塊：** checkbox-group 選項可依分類分組顯示（如使用機器依「歐洲／台灣／大陸／韓國」分區塊），不做可收合巢狀清單。
- **業務頁頂部新增接待資訊：** 接待人員、訪談日期、訪客國籍、語言四個欄位移至業務備註頁最上方，由業務在客戶交還平板後填寫。
- **潛力評級改為七級：** 取消 1–5 星評級，改為紙本原有的 A(100%)～G(0%看看而已) 七級單選。
- **感興趣產品填入完整機型分類：** Staff 的 Product of Interest 欄位預設值改為 Gentrex／EVA／橡膠系列等分組機型清單。
- **後台欄位編輯器整理：** Type 下拉新增單選按鈕組選項，選項列新增「Group」分類欄位，欄位清單顯示 `+Other` 標記。

## v3.0 更新重點

- **新流程：** 移除語言選擇入口，主畫面改為「客戶資料填寫 / 客戶需求填寫」二選一，依序完成後經「交接畫面」進入業務頁。
- **中英雙語排版：** 不再切換語言，所有欄位標籤、按鈕、錯誤提示、GDPR 聲明皆同時顯示英文（主）與中文（副）。
- **交接畫面防尷尬：** 客戶填完後畫面只顯示感謝與「請交還平板」，業務點擊角落按鈕才會進入評級頁，客戶不會看到評級介面。
- **名片拍照併入業務頁：** 評級、感興趣產品、備註、名片拍照在同一頁完成，按一次儲存全部寫入。
- **後台欄位拖曳排序：** 以把手拖曳調整欄位順序（Pointer Events 實作，支援 iPad 觸控），取代原本的上下箭頭按鈕。
- **可讀單檔重構：** 原始碼不再以 gzip+base64 打包，`index.html` 直接可讀可改。
- **真正離線：** React 已內嵌（先前版本會在執行時從 unpkg CDN 載入 React，首次開啟需要網路）。

### 資料相容性（v2 → v3 → v3.1 → v3.2 → v3.3 → v3.4 → v3.5 → v3.6 → v3.8 逐步自動遷移）

舊版資料庫會在開啟時自動遷移，已蒐集的紀錄不受影響：

- 「洽談事由（inquiry_type）」欄位定義自動從客戶欄位移至需求欄位；舊紀錄中存在客戶資料下的值仍可正常顯示與匯出。
- 「潛力評級（potential）」欄位定義自動從星級（或更早的 A/B/C 下拉選單）改為七級單選；舊紀錄既有的星數或字母值照原樣顯示，不做數值轉換。
- 接待人員／訪談日期／訪客國籍／語言四個欄位會自動加到 Staff Fields 最前面；若 Product of Interest 欄位仍是從未編輯過的空白預設值，會自動填入完整機型分類選項——**已自訂過的選項或其他業務欄位不會被覆蓋**。
- 新增 `companyFields` 資料表與預設欄位，既有紀錄的 Company Profile 資料視為空白，可事後從後台 Records 編輯補齊。
- 舊紀錄的語言標記（Language）保留於匯出資料中；新紀錄該欄為空白。
- **v3.2：** 「使用機器（machines_used）」欄位若尚未設定 `perGroupOther`，會自動補上（並新增「本土」空白分類），**既有的品牌選項清單完全保留、不被覆蓋**。「接待人員（greeter）」若尚未允許「其他」，會自動補上（既有的姓名選項不變）。「訪談日期（visit_date）」若仍是舊版單選按鈕組，會自動轉為新的 `date` 欄位類型；舊紀錄中已存的日期字串（如 `08/06`）維持原樣顯示，不做格式轉換。
- **v3.3：** 訪客國籍／語言自動移入客戶資料欄位，感興趣產品／成交機率／備註自動移入客戶需求欄位（依此順序），業務備註欄位只留下接待人員與訪談日期；「潛力評級」更名為「成交機率」（欄位 id 不變）。**搬移時沿用欄位原有的定義，後台自訂過的標籤與選項完全保留。** 此遷移由 `config` 中的 `migratedTo33` 旗標控制、只執行一次——因此日後在後台自行刪除這些欄位，不會在下次開啟時被重新加回。舊紀錄的值仍留在原本的 `staffFields` 中，讀取時會自動向前回溯，Records 與匯出照常顯示。
- **v3.4：** IndexedDB 由 v1 升到 v2，新增 `events` 資料表。開啟時若尚無任何活動，會先跑完上述整條遷移鏈，再把結果收編進一個預設活動：
  - 有既有紀錄時活動命名為「2026 美國展」（可在活動列表改名），日期區間自動取既有紀錄的最早與最晚日期；全新安裝則建立名為「新活動」、起訖為當天的活動。因此**開機後一定至少有一個活動可以進入**，不會因為「新增活動需要 PIN」而卡在空白首頁。
  - 活動的接待人員名單取自舊的 `greeter` 選項，**後台自訂過的姓名一併沿用**。
  - 四組欄位定義從無前綴的鍵搬到 `<活動 id>::<群組>`，舊鍵刪除；每筆既有紀錄補上 `eventId`。
  - `greeter` 轉為 `checkbox-group` 並標記 `source: 'event'`，`visit_date` 同樣標記。**舊紀錄中以字串儲存的單一接待人員原樣保留、不強制轉成陣列**——列表、編輯與匯出三處都同時支援字串與陣列。
  - 判斷依據是「`events` 是否為空」而非旗標，因此重新載入不會重複建立活動。
- **v3.5：** 洽談事由新增三個選項並開啟「其他」、公司型態新增代工廠。新建立的活動選「內建預設」時直接帶到；**既有的每一個活動則由 `migratedTo35` 旗標控制、一次性補上**。補的方式是以選項的 `en` 比對：已經有的不重複加、後台改過的中文標籤不覆蓋，只把缺的附加到選項末端。用旗標而非形狀比對，是因為「從未有過這個選項」與「管理者刻意刪掉它」在資料上分不出來，只有一次性的補寫才不會把刪掉的選項每次開啟都加回來。
- **v3.6：** 每個活動的接待人員名單由 `migratedTo36` 旗標控制、一次性**無條件覆蓋**為新的預設兩位（Esme／Crystal）。與 v3.5 的補寫相反，這裡不做合併也不比對是否被自訂過——舊的三個姓名是要移除而非被補充，合併只會把它們留下。**因此後台自訂過的名單也會被覆蓋**；遷移之後才改的名單則受旗標保護，不會在下次開啟時被打回預設。既有紀錄不受影響：紀錄存的是接待人員的英文字串本身，不是指向活動名單的參照。
  - 連帶影響：pre-v3.4 的資料庫升上來時，v3.4 會先把舊 `greeter` 選項（含自訂姓名）帶進第一個活動的名單，v3.6 隨即在同一次載入中覆蓋掉，**這條路徑上的自訂姓名不會保留**。
- **v3.8：** 由 `migratedTo38` 旗標控制、一次性執行兩件事。**一、** 每個活動的接待人員名冊覆蓋為實際團隊 8 人（同 v3.6，不比對是否被自訂過）。**二、** 訪客國籍由自由文字升級為 60 國可搜尋選單——**只在該欄位仍是 `text` 型別時才動**，也就是沒有人在後台改造過它；後台自訂的中文標籤保留。**既有紀錄完全不受影響**：`nationality` 一直存的就是純字串，而選單接受清單外的值，所以舊版填的 `Germany` 或 `TW` 依然有效、照常顯示與匯出。
