# 更新紀錄

會談紀錄表的版本沿革。最新版在最上。

各版的「為什麼」——踩到什麼、怎麼發現的——多半也寫在對應的 commit 訊息裡；
這份文件記錄結果與對既有資料的影響。

**發布新版時**：`index.html` 的版本字串與 `sw.js` 的 `VERSION` 必須一致，
否則新的 HTML 會沿用舊的快取名稱，所有平板都會停在舊版且無從察覺
（`tests/e2e-pwa.js` 有一條斷言擋這件事）。

---

## v3.16.5 更新重點 — 拆分 `renderVals()`（#12）

**純內部重構，畫面與資料一個字都沒變。** 唯一對使用者可見的差別是版本號。

`renderVals()` 是自有程式碼裡最長的方法。開 [#12](https://github.com/art20217/exhibition_form/issues/12) 時（v3.8）是 367 行，到 v3.16.4 已經 **579 行**——issue 說的「每加一個畫面就再長一截」八個月後應驗了。

拆成 `renderCtx()` 加 **15 個分段方法**，`renderVals()` 收斂成 **20 行的組合器**：

```js
renderVals() {
  const ctx = this.renderCtx();
  return { ...this.shellVals(ctx), ...this.eventListVals(ctx), … };
}
```

關鍵在於**回傳物件跟著一起拆**。原本方法尾端那個扁平的 return 佔了 198 行、將近三分之一；只把計算搬出去、return 留一大坨，下一個畫面照樣讓它長回來。現在每個分段自己回傳那一塊繫結，**新增一個畫面＝新增一個方法加一行**。

`renderCtx()` 收的是跨段共用的那一小把值（畫面旗標、`curEvent`、欄位擁有權、`editing`、步驟標籤），順帶把 `eventRecords()` 與三組 `sortedFields()` 各算一次——先前紀錄表格與瀏覽頁會各走一遍。另外把記錄列的取值抽成 `recordCellText()`，這兩個畫面從此不可能對同一格顯示什麼有分歧。

### 新增 `tests/e2e-render-vals.js`

這次重構只有一種失敗方式：**某個分段不再回傳某個鍵**。模板拿到 `undefined`，不會拋錯、不會有 console error，只是某顆按鈕沒有文字。既有的 32 支套件沒有一支擋得住。

所以新增一支：走訪 15 個畫面，把 **213 個繫結的鍵與型別**存成 `tests/render-vals.json` 比對。重構前後的快照**逐字相同**，這是這一版正確性的主要依據。

只比型別不比值——值本來就每次都不一樣（時間戳、生成的 id），而鍵從 `function` 變成 `undefined` 一定是 bug。日後真的新增繫結時以 `UPDATE_RENDER_VALS=1` 重跑並一併提交 fixture，是**刻意的動作**而不是漏網。

> 負面驗證：拿掉 `needsBackLabel` 一行 → 15 個畫面全部報「少了繫結 needsBackLabel」。

## v3.16.4 更新重點 — 狀態列不再報照片張數

實機驗證全數通過之後的一項收斂。

同步狀態列從 v3.14 起會多寫一句「待上傳照片 N 張」。拿掉它，只留「待同步 N 筆」。

理由是那個數字**回答不了這條列存在的唯一問題**——「有沒有東西還只在這台平板上？」照片會排隊，代表它的紀錄**早就送到伺服器上了**（未同步的紀錄根本不會傳照片，否則伺服器會收到孤兒圖檔）。所以照片還在排隊時，資料其實是安全的，那個數字只是多一個要解讀的東西。

**照片照常上傳，佇列本身沒有動。** 同步進行中的「上傳照片 3/5…」也留著——那是進度不是待辦：展場 Wi-Fi 下一張可能要好幾秒，沒有它整條列看起來像當掉了。

> 這一版順帶補上一條回歸斷言。原本**沒有任何測試碰過狀態列的照片字串**，所以那句話可以被刪掉而 32 支套件全綠。新的斷言帶一條正面對照（「此刻確實有照片排隊」）與一條存在性檢查（「狀態列真的在畫面上」）——少了任何一條，它都會在佇列是空的、或元素根本取不到的時候假性通過。

### 接管傳播的時間終於量到了

實機驗證第 4 輪量到了「A 被接管之後多久變唯讀」——這是 v3.15 上線以來文件一直缺的一塊。**在活動列表上都是一秒內**：按「立即同步」約 0.5 秒，切到別的 App 再切回來約 0.1 秒；停在填單畫面時不會變（刻意的）。已寫進 `README.md` 的已知限制與 `docs/wiki/功能總覽.md` 的雲端同步段落，驗證清單那張表也從「待填」改成「基準／本次」兩欄。

## v3.16.3 更新重點 — 增加聯絡人可以取消 ＋ 照片張數不再撐破欄寬

兩個都是 v3.16.0 帶進來的，也都要真的用過才看得見。

### 按了「增加聯絡人」之後有退路了

原本按下去就沒有回頭路：**「下一步」按不動**（必填欄位是空的），**「返回」會離開整個流程**——客戶資料是第一步時它直接回活動首頁，而第一位**已經存下來了**，卻還沒走到客戶需求與公司背景，於是後台留下一筆不完整的紀錄，得事後回填。

現在按鈕堆最上方多一顆**「取消，返回上一位」**，頁首的「返回」在有聯絡人停放時也做同一件事（標籤改成「← Back 返回上一位」）。按下去等於把剛才那次「增加聯絡人」收回來：畫面上這張還沒送出的表單丟掉，**上一位變回目前這筆**、資料連同名片照片一起讀回表單，人留在客戶資料頁。接著按「下一步」的行為與原本一模一樣。

想整個離開活動時按兩次：第一次回到上一位，第二次（此時已無停放的聯絡人）與今天相同。

> **取消鈕放在最上面，不是最下面。** 「下一步／完成」是這一頁——以及它前面每一頁——最底下那顆滿版實心鈕，那一格是靠習慣按的，不是靠讀的。把取消放到那裡（或它下面的任何位置）等於放進拇指預設會去的地方，而且會讓「下一步」在第一位與第二位的頁面之間換位置。`e2e-multi-contact.js` 有一條斷言守著「下一步永遠是堆疊最後一顆」。

### 資料紀錄的「操作」欄放得下四張照片的標籤

v3.16.0 給照片鈕加上張數（`📷 ×4`）之後，那顆鈕從 32px 變成 51px，而「操作」欄固定 132px、內距吃掉 28px，實際只放得下 104px。**只要任何一筆有兩張以上照片，整張表格就多出一條 7px 的水平捲軸**——1280／1024／900px 都一樣，與螢幕寬度無關。

欄寬照它的最壞情況重算為 **156px**。這一欄仍然是規則決定寬度、不由內容決定：`📷 ×4` 就是它的上界。既有的版面測試全部用沒有照片的紀錄，所以看不到這件事；現在植入了 0／1／4 張三種紀錄。

## v3.16.2 更新重點 — 資料紀錄顯示中英選項 ＋ 完成鈕取消淡化

### 已選的選項顯示中英並陳

紀錄存的是選項的英文值（`en` 是資料，`zh` 只是顯示），所以後台看紀錄時「接待人員」那格寫的是 `Charlene`、編輯頁的摘要列寫的是 `Request Quote`。但**看紀錄的是自己人，而自己人是用中文認這些選項的**。

現在資料紀錄的表格、手機卡片、「客戶資料紀錄」瀏覽頁，以及編輯紀錄時的摘要列，都顯示成 `Charlene 蘇秋菊`、`Request Quote 索取報價`。清單裡沒有的值（手寫的「其他」、自由輸入的國家、名冊上沒有的人名）原樣顯示，不硬湊。

> **匯出完全不受影響。** 中英並陳只發生在顯示層，匯出的儲存格仍是純英文的 `en`——那是下游程式讀的值。這條界線有一條斷言守著。

接待人員是最容易漏的一個：它的名冊存在**活動**上，欄位定義裡的選項是空的（`source: 'event'`），所以要另外從活動的名冊解析。

### 完成頁的「Done 完成，返回 →」不再淡化

那顆鈕當初刻意做成透明底、灰字，為的是不讓客戶誤觸。現在不需要了——**位置、尺寸、文字全部不動**，只換成 App 既有的次要動作配色（白底藍字藍框），與活動卡片的「編輯」「查看紀錄」同一套。

### 寫測試時抓到自己的一條虛斷言

「匯出不含中文」原本寫成 `!buf.includes('Charlene 蘇秋菊')`，而 `buf` 是把 ZIP 以 latin1 解出來的字串——中文是 UTF-8 位元組，**那個比對永遠不會成立**。把 bug 故意放回去（讓 `optionLabel` 混進 `resolveFieldValue`）時測試照樣綠，才發現。改成先把中文編成 latin1 再比對之後，同樣的故意破壞就會轉紅。

## v3.16.1 更新重點 — 第一次實機驗證撞到的三個問題

實機驗證清單第一次跑就命中了它存在的理由：兩個問題都是 headless Chromium **結構上測不到**的。

### 名片只能開相機，選不到相簿（[#24](https://github.com/art20217/exhibition_form/issues/24)）

兩個檔案輸入都寫了 `capture="environment"`。這個屬性在 iOS 上的意思**不是「預設開相機」，是「只能開相機」**——Safari 不再彈出「照片圖庫／拍照／瀏覽」，直接進相機。所以**事先拍好的、掃描的、同事傳來的名片，完全沒有辦法收進系統**。

拿掉之後兩條路都在，代價是拍照多一下點擊。桌面瀏覽器一律忽略 `capture`，`setInputFiles` 更是直接繞過，所以整套測試不可能抓到——現在有一條**靜態斷言**守著它。

### 同步 404，而「測試連線」說成功（[#25](https://github.com/art20217/exhibition_form/issues/25)）

平板由 GitHub Pages 自動更新，電腦上的 `server/` 只有在有人 `git pull` 時才更新。兩邊脫節時，**app 會去打伺服器還沒有的 `/v1/events`**。

而當時的程式讓這件事變成災難：活動同步排在紀錄之前，一個 404 就中止整個週期，**紀錄——真正的資料——根本沒送出去**；狀態列只說「伺服器錯誤（404）」，不講是哪個請求；而唯一的自助診斷「測試連線」只探 `GET /records`，所以它說「連線成功」。**現場唯一能自己判斷的工具，把人指向了錯誤的方向。**

四項修正：

- **參考伺服器記錄每一個請求**（`404 POST /v1/events  ← 失敗`）。終端機一直開著卻什麼都不說，這一行本來能讓問題五秒結案。
- **錯誤訊息帶上方法與路徑**：`伺服器錯誤（404 POST /events）`。
- **「測試連線」逐一探測每個實際會用到的端點**，逐項回報，並在全是 404 時明說「此伺服器缺少活動端點，版本可能較舊」。照片端點刻意不探——唯一的測法是 PUT 一張圖，而診斷不該寫資料。
- **活動端點 404 時降級而不是停擺**：記下來、本次工作階段不再嘗試、**紀錄照常同步**，狀態列說明「此伺服器不支援活動同步」。降級可以，靜靜降級不行。只有 404 有這個待遇，其他錯誤照常報錯。

這不只是修這一次的意外。**公司的正式伺服器一定是分階段實作的**——先有紀錄、後有活動——所以「app 比伺服器新」是接下來一段時間的常態，契約也跟著補上了這一條。

### 契約補一條：`OPTIONS` 要在路由之前回答

寫測試時撞出來的：我的假伺服器連預檢一起 404，結果瀏覽器以 CORS 擋下請求，**客戶端收到的是一個沒有狀態碼的網路錯誤**，分不出「端點不存在」和「網路斷了」，降級也就無從觸發。未實作的路徑也必須回 204。

### 清單頁的進度條不會動

`.progress-fill` 是 `<span>`，`display` 停在 `inline`，而 **inline 元素忽略 `width` / `height`**——寬度算對了、計數也跳了，就是那條 span 沒有盒子。外層軌道沒事，純粹因為它是 flex item 被 blockify 了。

## v3.16.0 更新重點 — 名片可拍多張 ＋ 同一組客戶多位聯絡人 ＋ 完整國碼清單

兩個直接來自展場的需求，外加一項技術債。

### 名片拍到四張，正反面都收得到

名片有背面，而背面常常才是有東西的那一面：產品線、地址、第二組電話。**這個 App 不做 OCR，所以名片照本身就是資料**——拍不到的那一面等於沒有蒐集到。

- 客戶資料頁尾改成一份照片清單，**最多 4 張**，每張各有「重拍」與「移除」（移除照舊兩層確認，名片正本已經跟著人走了，誤觸救不回來）。
- 後台資料紀錄的照片鈕標出張數（`📷 ×3`），燈箱一次顯示全部——只顯示第一張等於把背面藏起來。
- 匯出的檔名一律帶編號 `001_公司_姓名_1.jpg`、`_2.jpg`，**只有一張時也是 `_1`**：規則一致，下游不必處理兩種命名。

### 「增加聯絡人」：同一家公司來三個人，只填一次共同的答案

一家公司常常一次來兩三位。先前每一位都要把整份表單重走一次，包含公司背景那十個欄位，而客戶就站在旁邊等。

現在客戶資料頁底部多一顆「增加聯絡人」，**原本的「下一步」保留不動**。按下去之後這一位就寫定了，表單回到空白等下一位；**後面所有共同的回答只答一次，會落在這一組的每一筆上**。

- **只有公司欄位留著**，姓名、信箱、電話、國籍與名片照片都清空。留著 email 或電話比重打更糟——那種錯事後從紀錄上看不出來。同意條款也回到未勾：那份條文是給下一個人看的。
- **送出後是完全獨立的兩筆紀錄**，彼此沒有任何關聯欄位，後台也不會顯示他們是一起來的。
- 入口的兩種順序都成立。客戶資料是最後一步時（從客戶需求開始），共同答案早就填完了，後加的那位會**帶著**它們建立，而不是留空。
- 編輯既有紀錄時不會出現這顆鈕：編輯是修一筆，不是開一個流程。

### 電話國碼改成完整清單，而且搜得到（[#15](https://github.com/art20217/exhibition_form/issues/15)）

先前的國碼是一份**手寫的 35 筆**清單。展場常見的市場都在裡面，但**來自其他任何國家的訪客就沒有正確答案可選**——選單裡挑一個最接近的，事後從匯出檔上完全看不出那是錯的。

- 換成**完整的 E.164 清單（241 筆）**，由 `tools/make-calling-codes.js` 產生後內嵌。國家名稱取自 `Intl.DisplayNames`，與 v3.8 的國籍清單同一個來源，兩份清單不會對同一個國家有兩種叫法。
- 介面**比照國籍選單**改成可搜尋的下拉：打中文（`德國`）、英文（`Germany`）、數字（`49`、`+49`）或 ISO 代碼都找得到。241 筆是清單，不是選單，硬捲是不能用的。
- **一個與國籍不同的地方：這一欄不接受自由輸入。** 國籍存的是訪客怎麼稱呼自己的國家，而國碼會**直接接在電話號碼前面**——`TW 0912…` 不是電話號碼，而且事後沒有人分辨得出來。輸入不存在的國碼會退回原本的值。

清單是產生後貼進去的，所以 `e2e-calling-codes.js` 有一條會實際跑 `--check` 比對——沒有它，產生器過幾版就會變成虛構的文件。

### 同步契約升到 v3

照片端點的路徑參數從**紀錄 id 改成照片 id**（`PUT /v1/photos/<photoId>`），紀錄多帶一個 `photoIds`。這是破壞性變更，但正式伺服器還不存在，現在改最便宜。

**用陣列位置定位照片會在刪掉中間一張時整排位移**，伺服器上那張就成了永遠清不掉的孤兒。照片有自己的 id 之後，刪除在任何一端都不會算錯。而「刪掉哪一張」仍然由紀錄本身表達——伺服器刪掉該紀錄名下、不在 `photoIds` 裡的檔案，一如 v3.14 拒絕新增刪除端點的理由。

### 遷移 `migratedTo316`

`cardPhoto` 字串變成 `cardPhotos` 清單。**第一張刻意沿用紀錄 id 當照片 id**：伺服器上那個檔案本來就叫這個名字，沿用之後摘要仍然對得上，**已經傳上去的名片不會整批重傳一次**。展場的網路禁不起那個。

## v3.15.0 更新重點 — 活動與欄位定義同步（契約 v2）＋ 系統設定搬家

**兩台平板終於看得到彼此的紀錄。** 這是 v3.13、v3.14 都做不到的事——不是因為紀錄沒送達，而是因為活動沒送達：拉回來的紀錄帶著原本那台平板的 `eventId`，接收端沒有那場活動，於是那筆紀錄留在資料庫裡卻不出現在任何畫面。

同時，伺服器上的資料從此**看得懂**。先前每筆紀錄只有欄位 id 與選項的英文值，中文標籤、欄位型別、後台自訂欄位的名稱在伺服器上完全不存在——匯出檔本來就附一張 `Field Definitions` 工作表，理由完全相同。

- **活動與欄位定義快照一起推送**（`POST /v1/events`）。一個請求自我完備，伺服器不必再去拼湊。
- **獨立的游標 `syncEventSeq`**，與紀錄的序號空間分開。
- **活動在紀錄之前同步。** 先有活動，拉回來的紀錄才有歸屬。
- 拉取的時機規則不變（只在活動列表或活動內頁）。套用欄位定義等於把表單在人腳下換掉，這條規則現在更重要。

### 欄位由一台裝置說了算

每場活動記一個 `ownerDeviceId`，預設是建立它的那台。非擁有者的三個欄位頁籤**唯讀**，頂端說明由哪台裝置管理；**資料紀錄與匯出完全不受影響**，任何平板都照常收單。管理模式的活動卡片上有「接管欄位設定」，**一層確認**——接管是可復原的（對方可以再接管回去），依既有規則就是一層。

這樣做是為了讓欄位定義**完全不需要合併**。定義是巢狀結構，兩個人各加一個選項要怎麼合並沒有正確答案，只有幾種都會讓人失望的猜測。指定一個擁有者，衝突就從源頭消失。

> **這和後台 PIN 一樣是防誤觸，不是權限控制。** 共用權杖之下，伺服器分不出「刻意接管」與「冒名頂替」。要防的是同事在自己平板上改了欄位、把大家的設定蓋掉這種意外。

### 活動也有墓碑了

活動一旦會同步，原本的硬刪除就壞了——下次拉取會把它整個長回來，和紀錄在 v3.12 之前一模一樣的問題。現在刪除活動是墓碑；伺服器收到活動墓碑時，會一併刪掉該活動底下所有紀錄的照片，理由與 v3.14 的紀錄墓碑相同。

### 系統設定搬出「表單管理」

這一頁本來就是全域的（頁面自己寫著「此頁設定適用於所有活動」），卻要先進某一場活動才到得了——改個裝置名稱得先隨便走進一場展覽。現在改由**活動列表的齒輪**進入，同樣在管理模式（PIN）之後。是搬移不是複製：表單管理只剩四個頁籤，全部都是活動範圍的東西。

### 一個寫測試時撞出來的契約缺口

`e2e-event-sync.js` 早期版本用 2099 年的 `updatedAt` 去試探擁有者規則，結果那場活動**再也接受不了任何真實的修改**——LWW 之下，未來的時間戳會贏過此後每一次合法編輯，而且不報錯。與 v3.13 修掉的時鐘偏差是同一個家族，只是嚴重得多。客戶端不製造這種資料，但**正式伺服器應該拒收 `updatedAt` 遠超過自己現在時間的資料**——只有伺服器有一個可信的時鐘。已寫進契約的檢查清單。

### 遷移 `migratedTo315`

既有活動補上 `updatedAt`（回填為 `createdAt`，唯一已知的下界，比照 v3.12）與 `ownerDeviceId`（設為本機）。不可能撞號——目前各台平板的活動 id 都是各自產生的 UUID，本來就不共用。

## v3.14.0 更新重點 — 名片照片同步

紀錄同步在 v3.13 通過現場實測，但**照片仍留在平板上**——`forWire()` 刻意把 `cardPhoto` 拿掉。這讓同步只完成一半：**這個 App 不做 OCR，所以名片照本身就是資料**，少了它還是得逐台匯出，而那正是同步要消滅的手續。

- **照片走獨立端點上傳**（`PUT /v1/photos/<recordId>`，契約早已定義）。紀錄永遠先傳、照片後傳；照片失敗**不會**讓整次同步變成失敗狀態，因為紀錄已經安全送達了。
- **只替伺服器已接受過的紀錄上傳照片。** 先傳照片再傳紀錄，對方會收到一張沒有歸屬的圖。
- **以照片內容的摘要判斷是否重傳**，而不是 `updatedAt`。後者任何一次文字編輯都會前進，那會讓一張 200–500KB 的圖在改個錯字之後重飛一次——展場的網路禁不起。摘要用 FNV-1a 併長度，純 JS，因為 `crypto.subtle` 需要 secure context 而區網 http 的實測情境沒有。
- **一個同步週期最多傳 5 張**，還有剩就重新排一次。單張可能要好幾秒，不設上限會讓同步的單線鎖被佔住好幾分鐘，期間新填的紀錄推不出去。
- 狀態列加上「待上傳照片 N 張」與上傳進度。

### 修掉一個隱私漏洞：伺服器上的照片不會跟著刪

本機刪除紀錄時，墓碑會丟掉所有欄位值與照片，理由是**已刪除的紀錄不該還在平板上留著訪客的姓名、電話與名片照**。

同一個理由在伺服器那端完全成立，但先前沒有任何機制讓照片跟著消失——**刪掉的訪客，名片影像會永久留在伺服器上**。現在伺服器收到墓碑時會自己刪除對應的照片，並寫進契約當作實作者必須保留的行為。

刻意**不另設刪除端點**：刪除已經由紀錄本身表達了，而一個「要記得呼叫」的端點正是這類檔案最後永遠留著的原因。

> **照片只有單向。** 拉取不帶照片，所以另一台平板拉到的紀錄不會有圖——與「跨平板互看不是第一版的目標」一致。

## v3.13.0 更新重點 — 前景同步引擎（Phase 2 第二步）

紀錄同步真的接起來了。**名片照片仍未同步**（v3.14）；活動與欄位定義也還沒有（契約 v2）。

> **同步的範圍請先讀這段。** 這一版做到的是**每台平板的紀錄都會送達伺服器**——那是「多台平板資料合併」的核心。但**活動不同步**，所以從別台拉回來的紀錄帶的是**原本那台的 `eventId`**，接收端沒有對應的活動，那筆紀錄會留在資料庫裡卻**不出現在任何畫面、任何筆數、任何匯出**。
>
> 換句話說：**兩台平板還看不到彼此的紀錄。** 要做到那件事需要活動同步（契約 v2）。

- **前景同步。** iOS 從未實作 Background Sync API，所以觸發點是 app 啟動、切回前景、`online` 事件、手動「立即同步」，以及每次紀錄寫入後（去彈跳 2 秒，只推不拉）。單線執行，永不併發。
- **未設定伺服器時完全沒有動作**——這是預設值，同步狀態列整行不出現，行為與 v3.12 相同。
- **拉取只在活動列表或活動內頁進行。** 填單中與編輯中只推不拉：把拉回來的資料蓋在使用者正在打字的那筆上，會靜靜地毀掉他的輸入，而且不留痕跡。逐筆跳過會更糟——游標推進過一筆沒套用的紀錄，那筆就永遠不會再出現。
- **三種失敗分開顯示**：無法連線／存取權杖錯誤／伺服器錯誤。權杖錯不能顯示成連線失敗，否則使用者會跑去檢查 Wi-Fi。任何失敗都不遺失資料，未送出的紀錄維持待同步，下次觸發再試。
- **系統設定新增同步伺服器網址、存取權杖與「測試連線」。** 測試連線把「網路不通」和「權杖錯」分開，是展場現場唯一能自助診斷的工具。

> **伺服器網址必須是 https。** App 由 GitHub Pages 以 https 提供，瀏覽器會把對 `http://` 位址的請求當成 mixed content 直接擋掉——所以「筆電接在展場 Wi-Fi 上跑 http」這個直覺做法**不會動**。

### 修掉一個時鐘偏差造成的靜默資料遺失

本機修改原本一律以「現在」作為 `updatedAt`。但衝突是比 `updatedAt` 決定的，而**平板的時鐘會走偏**——從一台快幾秒的平板拉來的紀錄，其 `updatedAt` 可能還在未來。這時在本機刪除它，墓碑的時間比被刪的紀錄還「舊」，同步就判定墓碑落敗、丟掉它：**刪除靜靜地沒有生效，而且哪裡都不會報錯**。

現在本機的每一次修改都會把時間蓋過它所修改的那個值（`nextUpdatedAt()`）——動了這筆紀錄，就有權取代它。`tests/e2e-sync.js` 有一條刻意製造時鐘偏差的斷言守著；把修正拿掉會紅三條。

### 新增 `server/` 參考伺服器

零依賴的純 Node，JSON 檔儲存，實作契約全部三個端點（**含照片端點**，雖然這一版的客戶端還不會呼叫）。

它有兩個用途，第二個同樣重要：測試的對象，以及**交給日後實作正式伺服器的人，當一份可以執行的規格**。所以刻意不用任何平台原生服務——那會把契約綁在特定廠商的資料層上，而公司要自己重做一遍。部署方式與 Caddy 自動 TLS 範例見 [`server/README.md`](server/README.md)。

## v3.12.0 更新重點 — 同步契約與資料模型（Phase 2 第一步）

**這一版沒有任何網路功能。** 它把「同步需要哪些資料」變成事實，實際連線在下一版。
順序這樣排是因為軟刪除要掃過每一條讀取路徑，那不該和網路錯誤處理擠在同一次改動裡。

- **新增 [`docs/sync-contract.md`](docs/sync-contract.md)。** 寫給日後實作伺服器的人看的規格——公司端目前還沒有伺服器，所以我們定的契約要保守到別人能照著重做一次。重點：推送必須冪等（不穩的 Wi-Fi 下客戶端分不出「沒送到」和「回應沒回來」，一定會重送）、拉取用伺服器序號而非時間戳（多台平板時鐘不同步，用時間當游標會靜靜漏資料）、照片走獨立端點（單張 200–500KB，綁在紀錄裡會導致照片失敗連累紀錄永遠傳不完）。

- **紀錄新增 `updatedAt`。** `timestamp` 維持建立時間與既有語意（列表排序與匯出都靠它）；`updatedAt` 是同步比較新舊用的。既有紀錄由 `migratedTo312` 補成等於建立時間——那是唯一已知的下界，捏造一個更晚的時間會讓這台裝置在日後每一次衝突中不當勝出。

- **刪除改為墓碑（軟刪除）。** 同步必須分得出「這裡刪掉了」和「那裡還沒收到」，硬刪除做不到——已刪的紀錄會從沒同步的平板上重新長回來。
  - 墓碑只保留 `id` / `eventId` / `deviceId` / `timestamp` / `updatedAt` / `deletedAt`，**所有欄位值與名片照都會丟棄**：已刪除的紀錄不該還在平板上留著訪客的姓名、電話與照片。
  - 對使用者而言行為完全不變，兩層確認的「無法復原」依然誠實。
  - **`state.records` 現在含墓碑**，一律透過 `liveRecords()` / `eventRecords()` 讀取。

- **新增 `deviceId`**（一次性產生的 UUID，存在 `config`）。與系統設定裡可編輯的「裝置名稱」分開——那是人填的標籤，可以空白、可以兩台一樣，不能拿來當同步身分。

> **測試階段一律使用假資料。** 給訪客看的同意條文寫的是「本人資料不會提供予第三方」；上傳到該公司自己的伺服器沒問題，但開發或測試用的伺服器就是第三方。

## v3.11.0 更新重點

- **修正填單日期差一天（資料正確性問題）。** 活動設 09/23 開始，日期籤卻從 09/22 排起，整組往前一天；被選中的值會直接存進紀錄的「訪談日期」並帶進匯出。

  成因是日期字串經過 `toISOString()`：`new Date('2026-09-23T00:00:00')` 解析成**本地**午夜，`toISOString()` 再轉回 **UTC**，UTC+8 往回退 8 小時就跨過了日界。同樣的寫法也在「今天」的取得上，所以清晨 8 點前的預設日期也會是昨天。

  **只在 UTC 以東的時區發作**，這也是整套測試沒抓到的原因——CI 跑在 UTC。新增的 `tests/e2e-timezone.js` 明寫 `timezoneId`，並同時在 `Asia/Taipei` 與 `America/New_York` 各跑一次。

  > **既有紀錄不會自動修正。** 日期籤產生的值是「開始日−1 … 結束日−1」，落在活動期間內的偏移值和手動輸入的正確值長得一模一樣，程式無從分辨，硬改會把對的改錯。**請在客戶資料紀錄中逐筆核對並修正**（見下一項）。

- **紀錄編輯頁可以修改「訪談日期」。** 這個欄位由活動頁供應，先前在 App 內完全沒有修改途徑——日期一旦存錯就是永久的。接待人員與新舊客戶維持唯讀：它們記錄的是當下的選擇，事後改動沒有可信依據。

- **活動管理模式下，卡片不再是入口。** 管理模式提供的是編輯／結束／刪除，點卡片卻會直接進入表單頁，等於在管理情境裡藏了一個沒有標示的入口。現在卡片變成純資訊，要填單就先點頁首的「🔓 管理中（點此鎖定）」鎖回去。
  - 新增「**查看紀錄**」按鈕，落在表單管理的「資料紀錄」頁（含匯出）。**已結束的活動只在管理模式看得到、卡片又不可點，這是唯一的路徑**——「結束活動」對話框承諾的「資料與匯出仍可查看」靠它成立。
  - 從「查看紀錄」返回活動列表時會**還原管理模式**，否則剛看完的已結束活動會在返回的瞬間從列表消失。

- **國籍選定後仍看得到中文。** 選單本來就中英並陳，但選完關起來只剩英文。現在未聚焦時顯示「Taiwan 台灣」，點進去編輯則回到可輸入的英文值；編輯紀錄時的摘要列同樣中英並陳。
  - **儲存值與匯出仍是英文**（`Taiwan`）。選項的 `en` 是紀錄實際儲存的值，改它必須連既有紀錄一起改寫。

## v3.10.0 更新重點

- **填單入口重新設計：移除「快速填單」，改為「新／舊客戶」×「從哪張表開始」。** v3.8 的三顆並列按鈕把兩個彼此獨立的問題混在一起——**要不要填公司背景**，以及**從哪張表開始**。「快速填單」其實只是「舊客戶＋從客戶資料開始」這一種組合，卻和另外兩個平起平坐，所以看起來像三選一。
  - 活動頁新增「**客戶公司背景**」選擇：新客戶／舊客戶，底下註明「新客戶需填寫公司背景，舊客戶略過」。
  - 兩個入口更名為「**從客戶資料開始**」與「**從客戶需求開始**」，只決定順序。
  - 四種組合都到得了。**「舊客戶＋從客戶需求開始」是先前根本到不了的組合**。

| | 從客戶資料開始 | 從客戶需求開始 |
|---|---|---|
| **新客戶** | 資料 → 公司背景 → 需求（1/3） | 需求 → 公司背景 → 資料（1/3） |
| **舊客戶** | 資料 → 需求（1/2） | 需求 → 資料（1/2） |

- **修正兩步驟流程寫死了方向：** v3.8 的 `quickMode` 假設「客戶資料一定是第一步」——當時 `快速填單` 是唯一入口，所以成立。新組合「舊客戶＋從客戶需求開始」一走就露餡：步驟標籤顯示 `2 / 2` 而不是 `1 / 2`、送出按鈕寫成「完成」、而且送出後會轉回需求頁形成迴圈，紀錄根本存不進去。步驟標籤、按鈕文字與前後轉場現在一律依 `firstChoice` 推導。
- **新舊客戶會寫進紀錄並可匯出**（`Customer Status` 欄）。否則只能靠「公司背景是否空白」推測，而新客戶也可能因時間不夠而留白，兩者分不出來。
- **每填完一筆就重置新舊客戶**，填單人員與日期則維持不變。下一位訪客是新是舊每次都可能不同，黏著會默默記錯；人員一整天通常是同一位，重選只是白費工。

## v3.9.0 更新重點

- **編輯紀錄改為摘要列＋選項彈窗：** 編輯一筆紀錄時，客戶資料／客戶需求／公司背景三組欄位會串成同一頁——以預設欄位計算是 21 個欄位、**約 130 個選項按鈕**，改一個電話號碼得捲過所有機器品牌與行業別。現在**選項類欄位只顯示已選中的值**，右側一個「編輯」鈕開啟彈窗修改該欄位的選項。
  - **只折疊選項類欄位**（多選、單選、下拉、國籍選單）。文字、Email、電話、日期、備註、星等維持行內直接編輯——它們沒有選項可隱藏，改個錯字不該多點兩下。
  - 未填的欄位顯示灰字「未填」而不是消失，**快速填單留下的空白公司背景就是靠這裡補齊**。
  - 彈窗的「取消」會**還原整個欄位群組的快照**，不是只還原單一鍵——「其他」的文字存在 `<id>__otherText` 與 `<id>__otherText__<分類>` 這些側鍵，只還原一個鍵會留下殘值。
  - **客戶填單的三步驟流程完全不受影響**，選項照常全部攤開。測試有專門一條守住這件事：把填單頁也折疊起來會是退步，不是功能。

## v3.8.0 更新重點

- **App 更名為「會談紀錄表」**（`<title>`、PWA manifest、README）。主畫面圖示下的短名維持 `Exhibitions`。
- **新增「客戶資料紀錄」瀏覽頁：** 從活動內頁進入、需輸入 PIN。這是一個**獨立畫面，不是後台資料紀錄的捷徑**——接待人員可以搜尋（姓名／公司／接待人員）與編輯，但**沒有刪除、沒有匯出、沒有清除全部**。編輯完回到瀏覽頁而非後台，所以沒人會落在破壞性操作旁邊一步之遙。
  > 它與後台共用同一組 PIN，因此上述限制是**流程上的區隔而非權限上的**：拿到 PIN 的人一樣能自己進「表單管理」刪除或匯出。要真正分權需要第二組 PIN。
- **新增「快速填單」入口：** 只填客戶資料與客戶需求，**跳過公司背景**，步驟顯示 1 / 2、2 / 2。紀錄照常寫入，`companyFields` 留空，日後可從瀏覽頁或後台補齊。原本的兩個入口不變，仍是三步驟。
- **訪客國籍改為可搜尋的國家選單：** 60 個貿易往來國家，中英雙語，輸入即時過濾（打「德」或「Germ」都找得到德國）。**清單外的國名照樣可以直接輸入並存檔**，所以既有紀錄裡的自由文字值（如 `TW`）不會失效。儲存的是英文國名，與其他選項欄位一致。
- **齒輪按鈕加上文字標籤：** 活動列表為「活動管理」、活動內頁為「表單管理」。窄螢幕下只留圖示，避免擠壓活動名稱。
- **已結束的活動只在「活動管理」中顯示：** 未解鎖時整個「已結束」區塊不存在，日常使用不會被舊活動擋路。
- **預設填單人員改為實際團隊 8 人：** Charlene 蘇秋菊、Will 黃柏儒、Steve 陳誌翔、Nadia 鄭淑卿、Eric 顏耀中、Alen 黃世仰、Wing 張詩穎、Rick 張瑞育。由 `migratedTo38` 旗標一次性覆蓋所有既有活動（同 v3.6 的作法，後台自訂過的名單也會被覆蓋）。
- **活動編輯的日期欄位改為一行一格：** 並排時日期輸入框的原生選擇器有最小寬度，50% 的欄位容納不下，平板上會撐出 modal。

### 開發者注意：dc-runtime 沒有 pointer 事件

模板中寫 `onpointerdown` **不會生效**。`EVENT_MAP` 沒有這個項目，fallback 產生的是 `onPointerdown`（大小寫錯誤），React 直接忽略——處理器不會執行，也不會報錯。國籍選單的選項一度就是這樣靜靜地失效。可用的事件請參考 `EVENT_MAP`。

## v3.7.0 更新重點 — PWA 離線化（Phase 1 前半段）

- **可安裝為主畫面 App（standalone）：** 加入 `manifest.webmanifest` 與 icon（含 Android 圓形遮罩用的 maskable 版本），圖示沿用活動首頁頁首的日曆符號。iOS 的狀態列刻意維持不透明（`default`）——`black-translucent` 會讓白色狀態列文字疊在 body 的安全區留白上，而那塊是淺色頁面背景，等於看不見。
- **Service Worker 離線快取：** precache 就是整個站（`index.html` + manifest + icons），因為其他依賴全都內嵌在單一 HTML 裡。首次連網開啟後，完全離線也能正常啟動。
- **更新採橫幅、絕不自動重整：** 新版本安裝完成後**停在 `waiting` 不接管**（沒有 `skipWaiting()`）。橫幅只存在於活動首頁的區塊裡，**進入表單後它在 DOM 中根本不存在**——不是靠條件隱藏，是結構上不可能出現。客戶填到一半的畫面永遠不會在腳下被重整。
- **`?sw=off` 緊急停用（`?sw=on` 復原）：** 面對展場中拿不到手、卡在壞快取的平板，這是唯一的救援途徑。停用狀態**持久化**於 `localStorage`——第一版做成一次性的，結果解除註冊後 reload 又立刻註冊回去，若壞的正是 SW 本身等於毫無用處。停用期間活動首頁會標示，避免平板悄悄停在只能連線的狀態。**不會動到 IndexedDB**，紀錄完好。
- **只接管自己的入口路徑：** SW 不會把 scope 底下所有 navigation 都回答成 app 外殼——這個 app 只有一個入口、沒有前端路由，其他路徑照常走網路。

> **Background Sync 不在此版**。Phase 1 原訂的「偵測到網路後自動同步至後端」有兩重阻塞：後端屬於 Phase 2 尚未存在；而且 **Safari／iOS 從未實作 Background Sync API**，到 iOS 26.5 仍然沒有。展場裝置是 iPad，這條路在該平台不通，日後需改為前景同步（載入時、`visibilitychange` 回前景時、或 `online` 事件）。

### 測試基礎建設

- 16 支端到端測試與 CI **納入版控**（先前只存在於開發用的暫存目錄，曾整批遺失）。詳見下方「測試」一節。
- 新增 `e2e-pwa.js`（26 條斷言）：離線可用、更新橫幅不自動重整且不出現在表單頁、`?sw=off/on` 往返、紀錄不受影響、`index.html` 與 `sw.js` 的版本字串必須一致（`sw.js` 沒有 build step，這是唯一擋得住忘記 bump 快取版本的機制）。

## v3.6.1 更新重點

- **修正手機無法輸入後台 PIN 碼：** 點輸入框後鍵盤會跳出來，但框內完全沒有反應、盲打也進不去。原因是 v3.5 為了擋掉密碼管理圖示而加的一條規則：
  ```css
  #pin-input::-webkit-textfield-decoration-container { display: none !important; }
  ```
  `-webkit-textfield-decoration-container` **不是圖示**，而是 WebKit 用來包住輸入框 **inner editor**（真正可輸入的區域）的容器，隱藏它等於把可輸入區一起拿掉——欄位仍能取得焦點（所以鍵盤會跳出來），但沒有東西可顯示、也沒有東西可打字。已移除該行；針對兩個 autofill 按鈕的規則保留，那些是真正的裝飾按鈕。
- **為什麼桌機沒事、測試也沒抓到：** 桌機 Chrome 的密碼欄位沒有裝飾按鈕，而且 **Blink 根本沒有實作這個 pseudo-element**，規則等於空轉；iOS Safari 則會替密碼欄位加上 strong-password／autofill 按鈕，容器因此存在、欄位就壞掉。加上所有測試都用 Playwright 的 `fill()`——它直接指派 `.value` 再派發一次 `input` 事件，**完全不經過 inner editor**，所以就算在會壞的瀏覽器上也照樣通過。測試已全面改為逐鍵輸入（`pressSequentially`），並新增 `e2e-pin-mobile.js`（含一條「不得再出現這個選擇器」的靜態防護，因為 headless Chromium 重現不了 iOS 的實際行為）。

## v3.6 更新重點

- **預設接待人員換成 Esme（陳佩昀）、Crystal（宋佳蓉）：** 舊的三個姓名全部移除。
- **既有活動的名單由 `migratedTo36` 旗標一次性無條件覆蓋：** 名單是存在每個活動自己的 `staff` 裡的資料，改預設值不會動到已建立的活動，所以加一次性遷移把所有活動一律換成新名單。與 v3.5 的選項補寫不同，這裡是**整份取代而非合併**——舊的三個名字是要移除、不是被補充，合併只會把它們留著。**後台自訂過的名單也一併覆蓋**（這是刻意選的行為）。遷移**之後**才改的名單則受旗標保護，重新載入不會被打回預設。
- **既有紀錄不受影響：** 紀錄存的是接待人員的英文字串本身，不是指向活動名單的參照，所以在舊名單下填的紀錄，資料紀錄與匯出都照常顯示原本的名字。

> 注意：pre-v3.4 的資料庫升上來時，v3.4 原本會把舊 `greeter` 選項（含後台自訂的姓名）帶進第一個活動的名單，但 v3.6 緊接著在同一次載入中覆蓋掉——**這條路徑上的自訂姓名不會保留**。

## v3.5 更新重點

- **後台版面統一：** 欄位頁籤（800px）、資料紀錄（滿版）、系統設定（640px）三種不同寬度收斂為單一的 900px（`--admin-w`），頁籤列也跟著置中對齊，**第一個頁籤的文字與內容左緣落在同一條線上**。資料紀錄不再拉滿整個視窗，三個欄位欄各約 180px。
- **頁籤列改為捲動區內的 sticky 元素：** 傳統（非疊加式）捲軸會佔掉捲動容器的內容寬度，因此**內容夠長而出現捲軸的頁籤**（公司背景欄位、系統設定）會比其他頁籤往左偏約半個捲軸寬，看起來像寬度不一致。改為頁籤列與內容共用同一個捲動容器、以 `position: sticky` 固定在頂端，任何捲軸都會同時影響兩者；再加上 `scrollbar-gutter: stable` 永遠保留捲軸空間，切換頁籤時版面完全不跳動。
- **移除「業務備註欄位」頁籤與業務備註頁：** 接待人員與訪談日期已改由活動設定，這個頁籤沒有可管理的內容了。連帶移除交接後的業務備註畫面——交接畫面固定顯示「完成，返回」直接結束。既有紀錄中的舊值仍照常匯出。
- **活動首頁的版本號常駐底部：** 先前它跟在清單末端，活動只有一兩個時會浮到畫面上方；改為以 `margin-top: auto` 推到底，不論活動多寡都貼在最下方。
- **PIN 輸入框比例修正：** 原本固定 160px 配 2rem 字級與 12px 字距，四位數字本來就塞得很緊；部分裝置還會在框內加上顯示／密碼管理圖示而被撐爆。改為彈性寬度（上限 220px）、較小字級，並以 `text-indent` 補正字距造成的置中偏移，同時隱藏各瀏覽器自帶的密碼圖示。
- **洽談事由新增選項：** 機器瞭解、分享市場或同業訊息、打招呼聊天，並開啟「其他，請填寫」。
- **公司型態新增選項：** 代工廠（OEM Factory）。
- **修正 pre-v3.1 資料庫無法開啟：** 成交機率與感興趣產品搬到客戶需求群組後，v3.1 遷移仍到舊的 Staff 預設值裡找它們的選項，`find()` 回傳 `undefined`、讀 `.options` 直接拋錯，整個資料庫因此打不開。改為向 `getDefaultNeedsFields()` 查詢。只影響「成交機率仍是星等評分」或「感興趣產品仍是空白選項」的舊資料庫。

## v3.4 更新重點

**單一展覽的表單，變成可以重複用在每一場展覽的工具。**

- **活動首頁成為根畫面：** 開啟後先看到活動列表，分「使用中」與「已結束」兩區，每張卡片顯示活動名稱、日期區間、接待人員數與紀錄筆數。點進去才是該活動的表單。
- **新增／編輯／結束／刪除活動需要 PIN：** 展場平板放在客戶面前，管理按鈕預設隱藏，點右上角齒輪輸入 PIN 後才會出現。新增活動可設定活動名稱、活動日期區間、接待人員名單（英文＋中文兩欄），並選擇表單欄位來源——**內建預設**或**複製自某個既有活動**（複製的是獨立副本，日後修改互不影響）。
- **每場活動的欄位與資料完全獨立：** 欄位定義以 `<活動 id>::<欄位群組>` 為鍵分別儲存，每筆紀錄帶 `eventId`。後台的欄位設定、資料紀錄、匯出、「清除全部紀錄」一律只作用於目前開啟的活動。
- **填單人員與填單日期改在活動頁選擇：** 進入活動後，業務先選自己的名字（**可複選兩人以上**）與填單日期，才能點開兩個表單入口；**沒選就進不去**。填完自動帶入該筆紀錄，並在頁面上方持續顯示「填單人員：… ｜ 填單日期：…」與「重新選擇」，同一位業務連續接待時不必重選，換人時一眼看得出要改。
- **接待人員改為複選、由活動供應：** `greeter` 由單選改為多選，選項不再於後台編輯，改由活動的接待人員名單提供；`visit_date` 同樣改由活動頁帶入。兩個欄位定義仍保留（所以資料紀錄的「接待人員」欄與匯出欄位都還在），在後台標示為**由活動設定**、不可編輯或刪除。
- **業務備註頁只在還有業務專屬欄位時出現：** 上述兩欄搬走後，預設情況下業務備註頁已無內容，交接畫面直接顯示「完成，返回」。若在後台為該群組新增自訂欄位，業務入口會自動回來。
- **已結束的活動：** 兩個表單入口停用，但仍可進入後台檢視與匯出資料。結束／重新啟用是可復原的動作，只需一層確認；刪除活動仍是兩層＋輸入 `DELETE`。
- **匯出加上 Event 欄、檔名帶活動名稱：** `export_<活動名稱>_YYYYMMDD_HHMMSS.zip`。
- **系統設定維持全域：** 裝置名稱、管理者 PIN、GDPR 條文適用於所有活動。

## v3.3 更新重點

- **後台全面中文化：** 後台管理的所有介面文字（頁籤、欄位清單、欄位編輯器、資料紀錄、系統設定、提示訊息、表格標題、刪除確認）改為純中文。欄位類型以中文顯示（單行文字／單選按鈕組／多選核取組／日期…），紀錄表格改用欄位中文名稱。欄位的英文名稱仍保留並顯示為次要資訊——匯出欄名與選項比對仍依賴它。
- **所有刪除作業改為兩層確認：** 單筆欄位刪除、單筆紀錄刪除、清除全部紀錄、移除名片照片，四處全部改為「第 1 層說明要刪什麼 → 第 2 層再次確認無法復原」才會執行。先前單筆欄位與單筆紀錄是按下即刪、完全沒有確認。清除全部仍需先輸入 `DELETE` 才能進入第 2 層。
- **訪客國籍、語言移到客戶資料頁：** 這兩個欄位從業務備註頁移至客戶資料填寫頁。
- **感興趣產品、成交機率、備註移到客戶需求頁：** 客戶需求頁欄位順序為 洽談事由 → 感興趣產品 → 成交機率 → 備註。原「潛力評級」更名為**成交機率**（欄位 id 不變，舊紀錄的值照常顯示），七級 A～G 選項不變。
- **名片拍攝移到客戶資料頁尾：** 名片拍照從業務備註頁移至客戶資料填寫頁的頁尾（GDPR 同意聲明上方），與該頁配色一致。業務備註頁在搬移後只保留接待人員與訪談日期兩個欄位，交接畫面與整體流程維持不變。

> 註：成交機率現在位於客戶端表單上。此版本假設平板由業務人員操作或陪同填寫；若改為讓客戶單獨自行填表，客戶會看到這個內部評估欄位。

## v3.2 更新重點

- **三步驟流程順序調整：** Company Profile 公司背景資料改為**固定在第 2 步**（不論先選「客戶資料」或「客戶需求」），先選的維持第 1 步、後選的變成第 3 步。
- **checkbox-group 新增「分組獨立其他」機制（`perGroupOther`）：** 「使用機器」欄位的歐洲／台灣／大陸／韓國／新增「本土」五個分類子區塊，現在**各自擁有獨立的「其他，請填寫」**（先前僅整個欄位共用一個附加在最後）。此機制為通用能力，後台任何 checkbox-group 欄位皆可透過「每個分組獨立其他」勾選框與「額外空白分類」設定啟用（供未來其他分組欄位使用）。
- **接待人員新增「其他」：** 業務備註「接待人員」欄位保留既有三個姓名選項（後台仍可編輯），新增「其他，請填寫」因應臨時支援的人員。
- **新增通用 `date` 欄位類型：** 「訪談日期」由原本硬編三個日期的單選按鈕，改為原生日期選擇器；空白時預設當天日期，可自由調整。後台欄位編輯器 Type 下拉新增「Date」選項，供任何表單新增日期欄位使用。
- **選項按鈕固定寬度：** 短標籤選項（雙語合計 ≤40 字）改為固定寬度（200px）並排列成整齊網格；長文字說明選項（如規模型態）仍維持全寬整行顯示，不受影響。

## v3.1 更新重點

- **第三個客戶端表單「Company Profile 公司背景資料」：** 依 TIENKANG 展覽會談記錄表加入行業別、客戶類型、公司型態、公司產品、使用機器、股份型態、營業額、員工人數、產能、規模型態等公司背景欄位。非主畫面可選項，固定插入三步驟流程中（v3.2 起固定為第 2 步，見上）。
- **新增「單選按鈕組」(radio-group) 欄位類型：** 外觀與 checkbox-group 相同的大按鈕，但點選會自動取消其他選項，並可顯示長段說明文字（如規模型態）。
- **「其他，請填寫」：** checkbox-group 與 radio-group 皆可在後台開啟，選取「其他」後即時出現文字輸入框，資料與匯出皆以 `Other: <內容>` 呈現。
- **選項分類子區塊：** checkbox-group 選項可依分類分組顯示（如使用機器依「歐洲／台灣／大陸／韓國」分區塊），不做可收合巢狀清單。
- **業務頁頂部新增接待資訊：** 接待人員、訪談日期、訪客國籍、語言四個欄位移至業務備註頁最上方，由業務在客戶交還平板後填寫。
- **潛力評級改為七級：** 取消 1–5 星評級，改為紙本原有的 A(100%)～G(0%看看而已) 七級單選。
- **感興趣產品填入完整機型分類：** Staff 的 Product of Interest 欄位預設值改為 Gentrex／EVA／橡膠系列等分組機型清單。
- **後台欄位編輯器整理：** Type 下拉新增單選按鈕組選項，選項列新增「Group」分類欄位，欄位清單顯示 `+Other` 標記。

## v3.0 更新重點

- **新流程：** 移除語言選擇入口，主畫面改為「客戶資料填寫 / 客戶需求填寫」二選一，依序完成後經「交接畫面」進入業務頁。
- **中英雙語排版：** 不再切換語言，所有欄位標籤、按鈕、錯誤提示、GDPR 聲明皆同時顯示英文（主）與中文（副）。
- **交接畫面防尷尬：** 客戶填完後畫面只顯示感謝與「請交還平板」，業務點擊角落按鈕才會進入評級頁，客戶不會看到評級介面。
- **名片拍照併入業務頁：** 評級、感興趣產品、備註、名片拍照在同一頁完成，按一次儲存全部寫入。
- **後台欄位拖曳排序：** 以把手拖曳調整欄位順序（Pointer Events 實作，支援 iPad 觸控），取代原本的上下箭頭按鈕。
- **可讀單檔重構：** 原始碼不再以 gzip+base64 打包，`index.html` 直接可讀可改。
- **真正離線：** React 已內嵌（先前版本會在執行時從 unpkg CDN 載入 React，首次開啟需要網路）。

### 資料相容性（v2 → v3 → v3.1 → v3.2 → v3.3 → v3.4 → v3.5 → v3.6 → v3.8 逐步自動遷移）

舊版資料庫會在開啟時自動遷移，已蒐集的紀錄不受影響：

- 「洽談事由（inquiry_type）」欄位定義自動從客戶欄位移至需求欄位；舊紀錄中存在客戶資料下的值仍可正常顯示與匯出。
- 「潛力評級（potential）」欄位定義自動從星級（或更早的 A/B/C 下拉選單）改為七級單選；舊紀錄既有的星數或字母值照原樣顯示，不做數值轉換。
- 接待人員／訪談日期／訪客國籍／語言四個欄位會自動加到 Staff Fields 最前面；若 Product of Interest 欄位仍是從未編輯過的空白預設值，會自動填入完整機型分類選項——**已自訂過的選項或其他業務欄位不會被覆蓋**。
- 新增 `companyFields` 資料表與預設欄位，既有紀錄的 Company Profile 資料視為空白，可事後從後台 Records 編輯補齊。
- 舊紀錄的語言標記（Language）保留於匯出資料中；新紀錄該欄為空白。
- **v3.2：** 「使用機器（machines_used）」欄位若尚未設定 `perGroupOther`，會自動補上（並新增「本土」空白分類），**既有的品牌選項清單完全保留、不被覆蓋**。「接待人員（greeter）」若尚未允許「其他」，會自動補上（既有的姓名選項不變）。「訪談日期（visit_date）」若仍是舊版單選按鈕組，會自動轉為新的 `date` 欄位類型；舊紀錄中已存的日期字串（如 `08/06`）維持原樣顯示，不做格式轉換。
- **v3.3：** 訪客國籍／語言自動移入客戶資料欄位，感興趣產品／成交機率／備註自動移入客戶需求欄位（依此順序），業務備註欄位只留下接待人員與訪談日期；「潛力評級」更名為「成交機率」（欄位 id 不變）。**搬移時沿用欄位原有的定義，後台自訂過的標籤與選項完全保留。** 此遷移由 `config` 中的 `migratedTo33` 旗標控制、只執行一次——因此日後在後台自行刪除這些欄位，不會在下次開啟時被重新加回。舊紀錄的值仍留在原本的 `staffFields` 中，讀取時會自動向前回溯，Records 與匯出照常顯示。
- **v3.4：** IndexedDB 由 v1 升到 v2，新增 `events` 資料表。開啟時若尚無任何活動，會先跑完上述整條遷移鏈，再把結果收編進一個預設活動：
  - 有既有紀錄時活動命名為「2026 美國展」（可在活動列表改名），日期區間自動取既有紀錄的最早與最晚日期；全新安裝則建立名為「新活動」、起訖為當天的活動。因此**開機後一定至少有一個活動可以進入**，不會因為「新增活動需要 PIN」而卡在空白首頁。
  - 活動的接待人員名單取自舊的 `greeter` 選項，**後台自訂過的姓名一併沿用**。
  - 四組欄位定義從無前綴的鍵搬到 `<活動 id>::<群組>`，舊鍵刪除；每筆既有紀錄補上 `eventId`。
  - `greeter` 轉為 `checkbox-group` 並標記 `source: 'event'`，`visit_date` 同樣標記。**舊紀錄中以字串儲存的單一接待人員原樣保留、不強制轉成陣列**——列表、編輯與匯出三處都同時支援字串與陣列。
  - 判斷依據是「`events` 是否為空」而非旗標，因此重新載入不會重複建立活動。
- **v3.5：** 洽談事由新增三個選項並開啟「其他」、公司型態新增代工廠。新建立的活動選「內建預設」時直接帶到；**既有的每一個活動則由 `migratedTo35` 旗標控制、一次性補上**。補的方式是以選項的 `en` 比對：已經有的不重複加、後台改過的中文標籤不覆蓋，只把缺的附加到選項末端。用旗標而非形狀比對，是因為「從未有過這個選項」與「管理者刻意刪掉它」在資料上分不出來，只有一次性的補寫才不會把刪掉的選項每次開啟都加回來。
- **v3.6：** 每個活動的接待人員名單由 `migratedTo36` 旗標控制、一次性**無條件覆蓋**為新的預設兩位（Esme／Crystal）。與 v3.5 的補寫相反，這裡不做合併也不比對是否被自訂過——舊的三個姓名是要移除而非被補充，合併只會把它們留下。**因此後台自訂過的名單也會被覆蓋**；遷移之後才改的名單則受旗標保護，不會在下次開啟時被打回預設。既有紀錄不受影響：紀錄存的是接待人員的英文字串本身，不是指向活動名單的參照。
  - 連帶影響：pre-v3.4 的資料庫升上來時，v3.4 會先把舊 `greeter` 選項（含自訂姓名）帶進第一個活動的名單，v3.6 隨即在同一次載入中覆蓋掉，**這條路徑上的自訂姓名不會保留**。
- **v3.8：** 由 `migratedTo38` 旗標控制、一次性執行兩件事。**一、** 每個活動的接待人員名冊覆蓋為實際團隊 8 人（同 v3.6，不比對是否被自訂過）。**二、** 訪客國籍由自由文字升級為 60 國可搜尋選單——**只在該欄位仍是 `text` 型別時才動**，也就是沒有人在後台改造過它；後台自訂的中文標籤保留。**既有紀錄完全不受影響**：`nationality` 一直存的就是純字串，而選單接受清單外的值，所以舊版填的 `Germany` 或 `TW` 依然有效、照常顯示與匯出。
